task:
  name: "AUTH Deployment & Environment Configuration"
  repo_path: "feature-auth/tasks_bootstrap"
  goal: |
    Prepare deployment and environment scaffolding for the auth platform. Define environment variables for models, retries,
    feature flags, admin bootstrap credentials, and secrets management. Document startup ordering across services, edge gateway,
    workers, and migrations, and produce CI/CD configuration that provisions required infrastructure components and health
    checks per the architecture guidance.
  context:
    spec_file: "docs/architecture/AUTH_PLATFORM.md"
  outputs:
    - backend/
    - devops/
    - docs/
    - tests/
  tests: true
